---
import Snippet from "@/components/Articles/Snippet.astro";
---

<Snippet
  title="Storage Get Method"
  lang="go"
  link="https://github.com/dyxgou/redis/blob/0e2f5cc51758b244bad26c091381964543c7bf97/internal/storage/storage.go#L114"
  code={`func (s *Storage) Get(k string) (Value, bool) {
  // Locks the map
  s.mu.RLock()

  val, ok := s.storage[k]

  // Unlocks the map
  s.mu.RUnlock()

  if !ok {
    return nil, ok
  }

  return val, ok
}`}
/>
