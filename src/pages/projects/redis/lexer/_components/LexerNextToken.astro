---
import Snippet from "@/components/Articles/Snippet.astro";
---

<Snippet
  title="NextToken() First Part Definition"
  lang="go"
  link="https://github.com/dyxgou/redis/blob/b001cf06b36d743462da83c59a59f8c61809184f/pkg/lexer/lexer.go#L47"
  code={`func (l *Lexer) NextToken() token.Token {
  // Handling EOF 
  if l.ch == byte(token.EOF) {
    return token.New(token.EOF, "")
  }

  // Handling whitespaces
  l.skipWhitespaces()

  ...
}`}
/>
