---
import Snippet from "@/components/Articles/Snippet.astro";
---

<Snippet
  title="General Parse Function"
  lang="go"
  link="https://github.com/dyxgou/redis/blob/0e2f5cc51758b244bad26c091381964543c7bf97/pkg/parser/parser.go#L59"
  code={`func (p *Parser) parseCommand() (ast.Command, error) {
  switch p.curTok.Kind {
  case token.GET:
    return p.parseGetCommand()
  ...
  }

  return nil, fmt.Errorf(
    "command not supported. got=%d (%q)",
    p.curTok.Kind, p.curTok.Literal,
  )
}`}
/>
